'use strict';

/** copy configuration. */
module.exports = function () {
    return {
        config1: {
            expand: true,
            src: ['src'],
            dest: 'iets'
        }

    };
};