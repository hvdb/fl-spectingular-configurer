'use strict';

/** copy configuration. */
module.exports = function () {
    return {
        config2: {
            expand: true,
            src: ['iets'],
            dest: 'dest'
        }

    };
};